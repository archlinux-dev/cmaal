#
# /etc/pacman.conf
#
[options]
HoldPkg     = pacman glibc
#IgnorePkg   =
Architecture = auto
ParallelDownloads = 5

[core]
Include = /etc/pacman.d/mirrorlist

[extra]
Include = /etc/pacman.d/mirrorlist

#[multilib]
#Include = /etc/pacman.d/mirrorlist
